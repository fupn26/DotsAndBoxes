<Page x:Class="DotsAndBoxes.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:DotsAndBoxes.Views"
             mc:Ignorable="d" 
             Background="WhiteSmoke" Height="400" Width="600">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Style="{StaticResource MaterialDesignHeadline3TextBlock}" Margin="0 0 0 60"> Dots and Boxes </TextBlock>
        <Button x:Name="Load" Visibility="Collapsed" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="10" Margin="0 0 0 10" Click="Load_Click">Load Game</Button>
        <Button x:Name="New" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="10" Margin="0 0 0 10" Click="New_Click">New Game</Button>
        <Button x:Name="Quit" Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="10" Margin="0 0 0 0" Click="Quit_Click">Quit</Button>
    </StackPanel>
</Page>
